# Control plane service boundary. Serves the desktop app and the mobile
# client from one place, so both see the same tasks, approvals and activity.
fastapi
uvicorn
websockets

# Core assistant dependencies (cross-platform)
SpeechRecognition>=3.16.1
standard-aifc
pyttsx3==2.90
pyautogui>=0.9.54
pyscreeze>=1.0.1
pillow>=11.0.0
# Draws the desktop UI's line icons. Optional: without it the interface
# falls back to text labels instead of glyphs.
cairosvg>=2.7.0
psutil==5.9.8
customtkinter
requests

# Speech input. PyAudio needs system PortAudio headers.
# Windows: `pipwin install pyaudio`. Fedora: `sudo dnf install portaudio-devel`.
PyAudio==0.2.14

# Text to speech: online first, local Piper as fallback
edge-tts
pygame
piper-tts

# Screen awareness / OCR
pytesseract
# Screenshot text search with no Tesseract binary needed (ONNX, CPU).
# Powers find_text_on_screen / find_and_click_text inside browsers and
# canvas apps that UI Automation cannot see.
rapidocr-onnxruntime

# Web + knowledge lookup
wikipedia
googlesearch-python
duckduckgo-search
# 1.42.0 pinned greenlet 3.0.3, which has no Python 3.13 wheels; newer
# playwright releases accept greenlet 3.5+ and install cleanly on 3.13.
playwright>=1.42.0

# Google Workspace
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
python-dateutil

# Documents and long-term vector memory
PyPDF2==3.0.1
chromadb

# Clipboard
pyperclip==1.11.0

# Hands-free wake word
openwakeword
numpy

# Global interrupt hotkey. On Linux this needs root to read input devices,
# so the assistant degrades gracefully when it cannot be imported.
keyboard

# Windows-only integrations. Guarded by lazy imports in system_tasks.py,
# so the assistant still runs without them on Linux and macOS.
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.4.0; sys_platform == "win32"
uiautomation==2.0.29; sys_platform == "win32"

# Encrypts the credentials the control plane holds, so a copied control.db is
# not a copied set of credentials.
cryptography

# Receiving files from the phone: FastAPI needs this for multipart uploads.
python-multipart
